import { Typography } from "@mui/material";
import { Layout } from "../../components";

const LeavePage = () => {
  return (
    <Layout>
      <Typography> LeavePage</Typography>
    </Layout>
  );
};

export default LeavePage;
